apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: nginx-virtualservice
spec:
    hosts:
      - nginx-service
    http:
      - route:
        - destination:
            host: nginx-service
            subset: green
          weight: 60
        - destination:
            host: nginx-service
            subset: blue
          weight: 40
